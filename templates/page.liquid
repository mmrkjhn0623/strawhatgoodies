<div class="collection-banner flex-div">
  <h1>{{ page.title }}</h1>
</div>
<div class="container" style="margin:60px auto 100px auto;position: relative;">
  
<div>{{ page.content }}</div>
</div>
{% render 'newsletter' %}